""" 1. Счастливый билет🚌

Серийный номер билета на автобус - это шестизначное число, например 432190.
Счастливым билетом считается билет, у которого сумма первых трех цифр
серийного номера равно сумме последних трех цифр.
- Напишите функцию is_lucky_ticket(), которая принимает один аргумент
ticket_id (int) - серийный номер билета.
- Функция должна возвращать True, если билет является счастливым, и False
в противном случае.
Дополнительно: Напишите подробную строку документации для этой функции, а
также сделайте проверку на то, что ticket_id является целым числом (TypeError) и
это число шестизначное (ValueError). Если это не так, то программа должна
вбросить соответствующее исключение. p.s. Так можно делать и для всех
остальных функций."""


# def is_lucky_ticket(ticket_id):
"""Проверяет, является ли шестизначный номер билета счастливым.
    Счастливым билетом считается билет, у которого сумма первых трех цифр
    серийного номера равна сумме последних трех цифр.
    Параметры:
        ticket_id (int): Шестизначный серийный номер билета.
    Возвращает:
        bool: True, если билет счастливый, иначе False.
    Исключения:
        TypeError: Если ticket_id не является целым числом.
        ValueError: Если ticket_id не является шестизначным числом."""
    # Проверка типа
    # if not isinstance(ticket_id, int):
    #     raise TypeError("ticket_id должен быть целым числом.")
    #
    # # Проверка на шестизначность
    # ticket_str = str(ticket_id)
    # if len(ticket_str) != 6 or not ticket_str.isdigit():
    #     raise ValueError("ticket_id должен быть шестизначным числом.")
    #
    # # Разделение на две части и подсчет суммы
    # first_half = sum(int(digit) for digit in ticket_str[:3])
    # second_half = sum(int(digit) for digit in ticket_str[3:])
    #
    # return first_half == second_half


# # Примеры использования
# print(is_lucky_ticket(432190))  # False
# print(is_lucky_ticket(123321))  # True


""" 2. Числа Армстронга

Число Армстронга — это число, сумма кубов цифр которого равна самому числу
(например, 153 = 13 + 53 + 33).
- Напишите программу, которая вычисляет все числа Армстронга в
диапазоне от 100 до 1000 и выводит их на экран в виде списка (их должно
быть 4)."""

# def find_armstrong_numbers(start=100, end=1000):
""" Находит все числа Армстронга в заданном диапазоне.
    Числом Армстронга считается число, сумма кубов цифр которого равна самому числу.
    Параметры:
        start (int): Начало диапазона (включительно).
        end (int): Конец диапазона (не включительно).
    Возвращает:
        list: Список чисел Армстронга в указанном диапазоне."""
#     armstrong_numbers = []
#     for number in range(start, end):
#         digits = [int(digit) for digit in str(number)]
#         if sum(d ** 3 for d in digits) == number:
#             armstrong_numbers.append(number)
#     return armstrong_numbers
#
# # Вычисление чисел Армстронга в диапазоне от 100 до 1000
# armstrong_numbers = find_armstrong_numbers(1, 1000)
# print(armstrong_numbers)

""" 3. Цензор ❌
- Напишите функцию censor_print(), которая принимает один аргумент -
string (str).
- Функция печатает string в консоль, но при этом заменяет “запрещенные”
слова в этой строке на “***”. (Список с запрещенными словами должен
находиться внутри функции, составьте его сами)."""


# def censor_print(string):
#     # Список запрещенных слов
#     forbidden_words = ["плохое", "некрасивое", "нецензурное"]  # Пример списка
#     words = string.split()  # Разделяем строку на слова
#
#     # Проходим по каждому слову и заменяем запрещенные на "***"
#     censored_words = [word if word.lower() not in forbidden_words else "***" for word in words]
#
#     # Собираем строку обратно и выводим
#     print(" ".join(censored_words))
#
# # Пример использования
# censor_print("Это плохое слово и некрасивое тоже")


""" 4. D...

- Создайте словарь flashcards, где ключи являются “темами”, а значения -
это словари, где ключом является слово на английском языке, а значение -
его перевод на русский или любой другой язык.
Например:

- Программа должна (в бесконечном цикле) приветствовать пользователя и
предложить ввести одну из существующих тем, например “Привет! Выбери
сегодняшнюю тему для изучения: city, fruits... или введи выйти”
- Если пользователь ввел “выйти”, то программа выводит на экран “Но ты
приходи еще!”, выходит из бесконечного цикла и завершается.

- Если пользователь ввел что-то, кроме предложенных тем или слова
“выйти”, то программа должна просто попросить его ввести строку еще раз.
- Если пользователь ввел одну из предложенных тем, то начинается “игра”.
Программа создает счетчик правильных ответов и начинает
последовательно просить написать перевод каждого слова из выбранной
тематики, например “Как на русском будет apple?”.

o Если пользователь вводит правильный перевод, то программа пишет
“Верно!”, увеличивает счетчик правильных ответов на 1 и задает
следующий вопрос.
o Если пользователь вводит неверный ответ, то программа пишет
f“Неверно! Правильный перевод: {правильный перевод}”.
- После того, как закончились все вопросы, программа выводит на экран
количество правильных ответов и количество вопросов, например f“Ты
набрал {количество_верных_ответов} из {количество_вопросов}.”. После
этого программа возвращается к первоначальному вопросу про выбор
тематики.

Дополнительно, по желанию:
- Можно держать вопросы в отдельном модуле и импортировать в главную
программу. Или создать файл json и загружать вопросы из него. Так
программа будет выглядеть более структурированной и читаемой.
- Можно расширить функционал, например сохранять неправильные ответы
в список и в конце игры выводить их на экран и тд."""

import json

# Функция для загрузки данных из файла
def load_flashcards(file_name="flashcards.json"):
    with open(file_name, "r", encoding="utf-8") as f:
        return json.load(f)

def flashcard_game(topic, flashcards):
    score = 0
    total_questions = len(flashcards[topic])
    incorrect_answers = []

    for word, translation in flashcards[topic].items():
        user_translation = input(f"Как на русском будет {word}? ").strip().lower()

        if user_translation == translation:
            print("Верно!")
            score += 1
        else:
            print(f"Неверно! Правильный перевод: {translation}")
            incorrect_answers.append((word, translation))

    print(f"Ты набрал {score} из {total_questions}.")

    if incorrect_answers:
        print("Неправильные ответы:")
        for word, translation in incorrect_answers:
            print(f"{word} - {translation}")


def main():
    flashcards = load_flashcards()  # Загружаем словарь из файла

    while True:
        print(
            "Привет! Выбери сегодняшнюю тему для изучения: city, fruits, animals, colors, professions, sports или введи 'выйти'.")
        topic = input().strip().lower()

        if topic == "выйти":
            print("Но ты приходь еще!")
            break
        elif topic in flashcards:
            flashcard_game(topic, flashcards)
        else:
            print("Некорректный выбор, попробуй еще раз.")


if __name__ == "__main__":
    main()




