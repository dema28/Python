""" 1. Регистрация (часть 1)

    1. Напишите функцию registration(), которая принимает 2 аргумента -
username (строка) и password() (строка)
    2. Функция должна проверить username, а именно:
        a. username является строкой.
        b. Длина этой строки минимум 4 символа и максимум 15.
        c. username состоит только из букв.
    3. Если username некорректный, то функция должна вызвать исключение
ValueError.
    4. Функция должна проверить password, а именно:
        a. password является строкой.
        b. Длина этой строки минимум 8 символов и максимум 45.
        c. password состоит только из букв и цифр.
    5. Если password некорректный, то функция должна вызвать исключение
ValueError.
    6 . Если “регистрация” прошла успешно, то функция должна вернуть True.

    Дополнительно: создайте свой тип исключения RegistrationError и используйте
его вместо ValueError."""

# class RegistrationError1(Exception):
#     """Пользовательское исключение для ошибок регистрации."""
#     pass
#
# def registration1(username1: str, password1: str) -> bool:
#     # Проверка имени пользователя
#     if not isinstance(username1, str):
#         raise RegistrationError1("Имя пользователя должно быть строкой.")
#     if not (4 <= len(username1) <= 15):
#         raise RegistrationError1("Длина имени пользователя должна быть от 4 до 15 символов.")
#     if not username1.isalpha():
#         raise RegistrationError1("Имя пользователя должно содержать только буквы.")
#
#     # Проверка пароля
#     if not isinstance(password1, str):
#         raise RegistrationError1("Пароль должен быть строкой.")
#     if not (8 <= len(password1) <= 45):
#         raise RegistrationError1("Длина пароля должна быть от 8 до 45 символов.")
#     if not password1.isalnum():
#         raise RegistrationError1("Пароль должен содержать только буквы и цифры.")
#
#     # Если все проверки пройдены
#     return True
#
# # Пример использования
# try:
#     print(registration1("User123", "Pass1234"))  # Должно вызвать ошибку для имени пользователя
# except RegistrationError1 as e:
#     print(f"Регистрация не удалась: {e}")


""" 2. Регистрация (часть 2)

- Напишите программу, которая в бесконечном цикле запрашивает у
пользователя ввести логин и пароль и сохраняет их в соответствующие
переменные.
- Вызовите функцию registration из предыдущей задачи передав ей
введенные логин и пароль пользователя.
- Если в registration были переданы некорректные данные, то ваша
программа должна перехватить вызванное функцией исключение
RegistrationError и написать в консоль “Ошибка регистрации!”, а затем
снова попросить пользователя ввести данные.
- Если данные были введены корректно, то программа должна вывести
“Успешно!” и выйти из бесконечного цикла."""

# class RegistrationError(Exception):
#     """Пользовательское исключение для ошибок регистрации."""
#     pass
#
# def registration1(user_name: str, pass_word: str) -> bool:
#     # Проверка имени пользователя
#     if not isinstance(user_name, str):
#         raise RegistrationError("Имя пользователя должно быть строкой.")
#     if not (4 <= len(user_name) <= 15):
#         raise RegistrationError("Длина имени пользователя должна быть от 4 до 15 символов.")
#     if not user_name.isalpha():
#         raise RegistrationError("Имя пользователя должно содержать только буквы.")
#
#     # Проверка пароля
#     if not isinstance(pass_word, str):
#         raise RegistrationError("Пароль должен быть строкой.")
#     if not (8 <= len(pass_word) <= 45):
#         raise RegistrationError("Длина пароля должна быть от 8 до 45 символов.")
#     if not pass_word.isalnum():
#         raise RegistrationError("Пароль должен содержать только буквы и цифры.")
#
#     # Если все проверки пройдены
#     return True
#
# # Бесконечный цикл для регистрации пользователя
# while True:
#     try:
#         # Запрос логина и пароля у пользователя
#         username = input("Введите имя пользователя: ")
#         password = input("Введите пароль: ")
#
#         # Вызов функции регистрации
#         if registration1(username, password):
#             print("Успешно!")
#             break
#     except RegistrationError as e:
#         print(f"Ошибка регистрации! {e}")

""" 3. Дорогой дневник...
- Создайте пустой текстовый файл journal.txt
- Программа должна в бесконечном цикле запрашивать пользователя
ввести строку, которая является одним из режимов: “прочитать”,
“записать”, “выйти”.
- Если пользователь ввел “записать”:
o Программа просит пользователя ввести еще одну строку, которая
будет записана в файл.
o Программа дозаписывает эту строку в файл journal.txt c новой
строки.

- Если пользователь ввел “прочитать”:
o Программа выводит на экран все содержимое файла journal.txt.
- Если пользователь ввел “выйти”:
o Программа пишет в консоль “Еще увидимся!”, выходит из
бесконечного цикла и завершается.
- Если пользователь ввел что-то другое:
o Программа ничего не делает, просто возвращается к следующей
итерации бесконечного цикла."""

# # Создаем (если не существует) пустой файл journal.txt
# with open("journal.txt", "a"):  # Открываем в режиме добавления, чтобы файл создался
#     pass
#
# while True:
#     # Запрашиваем у пользователя режим работы
#     mode = input("Введите режим (\"прочитать\", \"записать\", \"выйти\"): ").strip().lower()
#
#     if mode == "записать":
#         # Запрашиваем строку для записи
#         entry = input("Введите строку для записи в журнал: ")
#         with open("journal.txt", "a") as file:
#             file.write(entry + "\n")  # Записываем строку с новой строки
#
#     elif mode == "прочитать":
#         try:
#             # Читаем содержимое файла и выводим на экран
#             with open("journal.txt", "r") as file:
#                 content = file.read()
#                 print("\nСодержимое журнала:\n")
#                 print(content if content else "(Файл пуст)")
#         except FileNotFoundError:
#             print("Файл журнала не найден.")
#
#     elif mode == "выйти":
#         print("Еще увидимся!")
#         break
#
#     else:
#         # Игнорируем некорректный ввод
#         continue


""" 4. Бонус (Эта задача необязательна 😁)

В папке с домашним заданием есть файл secret.json, который содержит словарь с
одним ключом pixels. Значение по этому ключу — это список, содержащий

вложенные списки. Попробуйте изучить эти данные и придумать, что с ними
можно сделать."""

# import json
#
# # Загрузка данных
# with open("secret.json", "r") as file:
#     data = json.load(file)
#
# pixels = data.get("pixels", [])
#
# # Визуализация ASCII-графики в консоли
# print("ASCII-графика:")
# for row in pixels:
#     line = "".join(symbol * count for symbol, count in row)
#     print(line)
#
#
# # 2. Вариант
#
# import json
#
# # Загрузка данных
# with open("secret.json", "r") as file:
#     data = json.load(file)
#
# pixels = data.get("pixels", [])
#
# # Изменение данных: заменяем '#' на '*'
# updated_pixels = []
# for row in pixels:
#     updated_row = [(symbol.replace("#", "*"), count) for symbol, count in row]
#     updated_pixels.append(updated_row)
#
# # Сохраняем обновленные данные в новый файл
# updated_data = {"pixels": updated_pixels}
# with open("updated_secret.json", "w") as file:
#     json.dump(updated_data, file, indent=4)
#
# print("Данные успешно изменены и сохранены в 'updated_secret.json'")


""" 5. Повторение прошлого материала. 
    Ответьте на следующие вопросы:
        1. Что такое класс и что такое экземпляр класса?
        2. Какие методы http-запросов вы знаете? Что делает метод PATCH?
        3. Какие типы данных вы знаете? Чем отличается список от кортежа?
        
    Ответы: 
    1. Что такое класс и что такое экземпляр класса?
Класс — это шаблон или описание объекта, которое задаёт его свойства (атрибуты) и поведение (методы). В Python класс создаётся с помощью ключевого слова class.

class Car:
    def __init__(self, brand, model):
        self.brand = brand
        self.model = model

    def drive(self):
        print(f"{self.brand} {self.model} едет!")
Экземпляр класса — это объект, созданный на основе класса, представляющий конкретную реализацию шаблона.

my_car = Car("Toyota", "Corolla")  # Экземпляр класса Car
my_car.drive()  # Вывод: Toyota Corolla едет!

    2. Какие методы HTTP-запросов вы знаете? Что делает метод PATCH?
Основные методы HTTP-запросов:
GET: Получение данных с сервера.
POST: Отправка данных на сервер для создания ресурса.
PUT: Полное обновление ресурса на сервере.
DELETE: Удаление ресурса с сервера.
PATCH: Частичное обновление ресурса на сервере.
HEAD: Получение метаданных ресурса, без тела ответа.
OPTIONS: Получение информации о доступных методах для ресурса.
PATCH: Метод используется для внесения изменений в ресурс, не затрагивая его полностью. Например, если у ресурса есть множество полей, а нужно обновить только одно, то предпочтительнее использовать PATCH, чем PUT.

PATCH /users/1 HTTP/1.1
Content-Type: application/json

{
    "email": "new_email@example.com"
}
    3. Какие типы данных вы знаете? Чем отличается список от кортежа?
Основные типы данных в Python:
Числа: int, float, complex.
Логический тип: bool (True, False).
Строки: str.
Последовательности: list (списки), tuple (кортежи), range.
Множества: set, frozenset.
Словари: dict.
Байтовые типы: bytes, bytearray, memoryview.

Список (list) и кортеж (tuple):

Список:
Изменяемый (можно добавлять/удалять элементы).
Используется, когда нужно часто изменять данные.
Создаётся с помощью квадратных скобок [].

my_list = [1, 2, 3]
my_list.append(4)
print(my_list)  # [1, 2, 3, 4]

Кортеж:
Неизменяемый (нельзя добавлять/удалять элементы после создания).
Используется, когда данные должны оставаться неизменными.
Создаётся с помощью круглых скобок ().

my_tuple = (1, 2, 3)
print(my_tuple[0])  # 1 """


